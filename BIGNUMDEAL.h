// This header file gives us the ability to deal with large numbers

// this below function adds two given strings and returns, it adds each digit like basic addition;
#include <string>

std::string add_string(string s1, string s2);
